doctype html
head
  meta(charset='UTF-8')
  meta(name='viewport', content='width=device-width, initial-scale=1.0')
  meta(http-equiv='X-UA-Compatible', content='ie=edge')
  title Availability Form
  link(rel='stylesheet' href='css/login.css')
  link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous")
  style.
    body {
    background-color:lightgray;
    }
body
  .col-sm-5.mx-auto.my-12
    .card.text-center(style='background-color:rgb(132, 194, 173);')
      .card-header.d-inline-flex.mx-auto
            form(method="POST" action='/logout')
              button.btn.btn-style.btn-light(type='') Logout
            form(method="GET" action='/seeapprovedlist')
              button.btn.btn-style.btn-dark(type='none') Cancel
      .card-body
        h5.card-title UFARM AVAILABILITY FORM
        form.tm-login-form(action='/produce/available/?id='+item._id name='productupdate' method='POST')
          .form-group
            label(for='productimage') #{item.productname}:  .
            img.rounded(src="/"+item.productimage, alt="Image" width='150px' height="150px") 
          .form-group
            label(for='availability') Change to
            select.rounded(name='availability')
                option(value="available" selected = item.availability === "available") Available
                option(value="booked" selected = item.availability === "booked") Booked
                option(value="N/A" selected = item.availability === 'N/A') N/A
          .form-group
            button.btn.btn-dark.d-inline-block.mx-auto(type='submit') Change
          .form-group
            p
              em press change to save changes
      footer.row.tm-mt-big
        .col-12.font-weight-light.text-center
          p.d-inline-block.tm-bg-black.text-white.py-2.px-4
            | &copy; Massajja Urban farmers initiative. All rights reserved
      .card-footer.text-muted.mx-auto
        | market at your door

      
      
      
